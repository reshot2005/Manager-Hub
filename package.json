{
  "name": "manager-hub",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Manager AI Hub — Vercel frontend + serverless API",
  "scripts": {
    "dev:api": "npm run dev --prefix backend",
    "dev:web": "npm run dev --prefix frontend",
    "db:init": "npm run db:init --prefix backend",
    "seed": "npm run seed --prefix backend",
    "sync": "npm run sync --prefix backend",
    "build": "npm run build --prefix frontend",
    "vercel-build": "npm run build --prefix frontend"
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.0",
    "bcryptjs": "^3.0.2",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.5",
    "dotenv": "^16.6.1",
    "express": "^5.1.0",
    "express-rate-limit": "^7.5.0",
    "helmet": "^8.3.0",
    "jsonwebtoken": "^9.0.2",
    "node-cron": "^4.6.0",
    "pg": "^8.13.3",
    "serverless-http": "^3.2.0"
  }
}
